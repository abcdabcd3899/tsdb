" do not convert tabs to spaces
set noexpandtab

" tab stop is 4-character width
set tabstop=4

" indent 4 characters for each level
set shiftwidth=4

" put a guard line at column 80
set colorcolumn=80

" align at the (
set cinoptions=(0
